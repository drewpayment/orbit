// Stub for the `server-only` package under vitest. The real package is a
// build-time marker that throws if imported into a client bundle; it has no
// runtime behavior, so an empty module is a faithful test double.
export {}
