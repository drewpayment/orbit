{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "api:schema:register input",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Display name for the API schema."
    },
    "schemaType": {
      "type": "string",
      "enum": ["openapi", "graphql", "proto"],
      "description": "Contract style of `content`."
    },
    "content": {
      "type": "string",
      "minLength": 1,
      "description": "The starter spec text (OpenAPI 3.1 document, GraphQL SDL, or .proto source).",
      "ui:widget": "textarea"
    },
    "description": {
      "type": "string",
      "description": "Optional description shown on the schema's catalog page."
    },
    "visibility": {
      "type": "string",
      "enum": ["private", "workspace", "public"],
      "default": "workspace",
      "description": "Who can view this API schema in the catalog. Defaults to workspace."
    }
  },
  "required": ["name", "schemaType", "content"],
  "additionalProperties": false
}
